// Next.js 16 configuration for NaukrAI
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  typescript: {
    tsconfigPath: './tsconfig.json',
  },
}

module.exports = nextConfig
